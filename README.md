# Brain Dump
A place to unload thoughts and erase them.

---
## Background
This is a small project I wanted to make for myself after getting 3 Amazon edge consideration emails LOL. Why not use Notepad? Idk, I just didn't want to have to press "Don't Save" everything I close out of it...and I lowkey just didn't want to search up how to make it so it doesn't pop up every time. I also haven't worked with Java in a hot minute, so it's good to get a refresher while having a place where I can just discard my thoughts. TvT I hope next year I get more interviews...RANT OVER

## Things I learned or relearned:
- I actually never learned to create an executable .jar file, but to do so, you need to create a .txt file and run `jar cvfm BrainDump.jar (name).txt bd_app.class`
- You need to compile Java every time: `javac <file_name>`
- There will be two files: .java and .class
- No () in class
- To have to actually run, I needed this entire thing in Target for shortcuts: `"C:\Program Files\oracleJdk-26\bin\javaw.exe" -jar "C:\github projects\brain-dump\BrainDump.jar"`